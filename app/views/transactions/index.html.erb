<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Transactions</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
  <%= stylesheet_link_tag "transactions" %>
</head>
<body>
  <div class="container2">
    <h1>Transactions</h1>
    <p>
      Welcome, <%= User.find(session[:user_id]).name %>.
    </p>
    <p>
      Viewing transactions for UIN: <%= User.find(session[:user_id]).uid %>
    </p>

    <table class="table table-striped">
      <thead class="thead-dark">
        <tr>
          <th>UIN</th>
          <th>Type</th>
          <th>Time</th>
          <th>Amount</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% @transactions.each do |transaction| %>
          <tr>
            <td><%= transaction.uid %></td>
            <td><%= transaction.transaction_type %></td>
            <td><%= transaction.time %></td>
            <td><%= transaction.amount %></td>
            <td>
              <%= link_to "View transaction", transaction_path(transaction), class: 'btn btn-info btn-sm' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <div class="row btn-back">
      <%= link_to 'Back to user', '/users/profile', class: 'btn btn-primary col-md-2' %>
    </div>
  </div>

  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</body>
</html>
