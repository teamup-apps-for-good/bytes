<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Transactions</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
  <%= stylesheet_link_tag "transactions" %>
  <%= stylesheet_link_tag "https://fonts.googleapis.com/css2?family=Protest+Riot&family=Quicksand:wght@300..700&display=swap", media: 'all' %>
</head>
<body>
  <div class="outerDiv">
    <p class="title">Transactions</p>
    <p class="char">
      Viewing transactions for UIN: <%= User.find(session[:user_id]).uid %>
    </p>

    <table class="transTable">
      <thead class="thead-dark">
        <tr>
          <th>UIN</th>
          <th>Type</th>
          <th>Time</th>
          <th>Amount</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% @transactions.each do |transaction| %>
          <tr>
            <td><%= transaction.uid %></td>
            <td><%= transaction.transaction_type %></td>
            <td><%= transaction.created_at %></td>
            <td><%= transaction.amount %></td>
            <td>
              <%= link_to "View transaction", transaction_path(transaction), class: 'actionBtn' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</body>
</html>
